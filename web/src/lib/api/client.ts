const API_URL=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3000/api/v1';
export class ApiError extends Error{constructor(message:string,public status:number){super(message)}}
type ApiEnvelope<T>={success:boolean;data:T;meta?:{page:number;pageSize:number;totalItems:number;totalPages:number};message?:string};
let refreshPromise:Promise<boolean>|null=null;
const storage={get:(key:string)=>typeof window==='undefined'?null:localStorage.getItem(key),set:(key:string,value:string)=>localStorage.setItem(key,value),clear:()=>{localStorage.removeItem('accessToken');localStorage.removeItem('refreshToken')}};
async function refresh(){const token=storage.get('refreshToken');if(!token)return false;const res=await fetch(`${API_URL}/auth/refresh`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({refreshToken:token})});if(!res.ok){storage.clear();return false}const body=await res.json() as ApiEnvelope<{accessToken:string;refreshToken:string}>;storage.set('accessToken',body.data.accessToken);storage.set('refreshToken',body.data.refreshToken);return true;}
async function request<T>(path:string,options:RequestInit={},retry=true):Promise<ApiEnvelope<T>>{const headers=new Headers(options.headers);headers.set('Content-Type','application/json');const token=storage.get('accessToken');if(token)headers.set('Authorization',`Bearer ${token}`);const res=await fetch(`${API_URL}${path}`,{...options,headers});if(res.status===401&&retry&&!path.startsWith('/auth/')){refreshPromise??=refresh().finally(()=>{refreshPromise=null});if(await refreshPromise)return request<T>(path,options,false)}const body=await res.json().catch(()=>({message:'Request failed'}));if(!res.ok)throw new ApiError(body.message??'Request failed',res.status);return body;}
export const api={get:<T>(path:string)=>request<T>(path),post:<T>(path:string,data:unknown)=>request<T>(path,{method:'POST',body:JSON.stringify(data)}),patch:<T>(path:string,data:unknown)=>request<T>(path,{method:'PATCH',body:JSON.stringify(data)}),delete:<T>(path:string)=>request<T>(path,{method:'DELETE'}),tokens:storage};
